import { Box } from '@mui/system';
import ShoppingCartIcon from '@mui/icons-material/ShoppingCart';

function Cart() {
    return (
        <Box ml={2}>
            <ShoppingCartIcon />
        </Box>
    );
}

export default Cart;